name: Build Competitor History 2025

on:
  workflow_dispatch:
  schedule:
    - cron: "0,30 * * * *"

jobs:
  build-history-2025:
    runs-on: ubuntu-latest

    permissions:
      contents: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: Setup Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.11"

      - name: Build competitor history 2025
        run: python history_builder_2025.py

      - name: Save history
        run: |
          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"

          git add data/competitor_history.json data/history_cursor_2025.json

          if git diff --cached --quiet; then
            echo "No changes"
          else
            git commit -m "Update competitor history 2025"
            git pull --rebase origin main
            git push origin main
          fi
